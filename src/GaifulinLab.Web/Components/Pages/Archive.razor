@page "/archive"

<PageTitle>@L["catalog.archive.title"] · Gaifulin Lab</PageTitle>

<PublicContentIndex Kind="archive" LanguageCode="@L.CurrentCode" Eyebrow="@L["catalog.archive.eyebrow"]" Title="@L["catalog.archive.title"]"
                    Description="@L["catalog.archive.description"]"
                    EmptyTitle="@L["catalog.archive.emptyTitle"]" EmptyDescription="@L["catalog.archive.emptyDescription"]" />
